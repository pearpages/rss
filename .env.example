# Environment Variables Example
# Copy this file to .env and update the values as needed
# Only variables prefixed with VITE_ are available in the client

# Built-in Vite Variables (automatically set, no need to define):
# VITE_DEV - true in development, false in production

# Example custom variables (uncomment and set values as needed):
# VITE_API_BASE_URL=https://api.example.com
# VITE_RSS_PROXY_URL=https://api.rss2json.com/v1/api.json
# VITE_DEBUG_MODE=false
